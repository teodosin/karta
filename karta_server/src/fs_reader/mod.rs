use std::path::PathBuf;

use crate::prelude::Node;




fn desctucture_file_path(
    path: &PathBuf,
    include_root: bool,
) -> Vec<Node> {
    let mut nodes: Vec<Node> = Vec::new();

    return nodes;
}