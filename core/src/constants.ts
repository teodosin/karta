export const RUNTIME_VERSION = "0.1.0";

export const VIEWPORT_DEFAULTS = {
  minScale: 0.1,
  maxScale: 8,
  zoomStep: 1.1
} as const;

export const ANIMATION = {
  durationMs: 180,
  ease: "ease-out"
} as const;