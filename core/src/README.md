Source layout for @karta/core (Phase 1)
- constants.ts
- types/
- utils/
- viewport/
- stores/
- themes/ (placeholder)

These modules are intentionally editor-agnostic and MIT-licensed. Svelte store types will resolve after workspace install: pnpm -w i