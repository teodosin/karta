{
  "name": "@karta/core",
  "version": "0.0.0",
  "private": false,
  "license": "MIT",
  "description": "Core shared foundations for Karta runtime and editor: types, utils, viewport math, themes, constants, and base stores.",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "lint": "echo \"No linter configured yet\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.5.4",
    "svelte": "^4.2.18"
  },
  "peerDependencies": {
    "svelte": "^4.0.0 || ^5.0.0"
  }
}