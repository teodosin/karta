/**
 * Runtime store placeholders.
 * Concrete implementations will extend readonly base stores from @karta/core.
 */

export interface RuntimeNodeStore {
  // later: get nodes/readable views only
}

export interface RuntimeContextStore {
  // later: get current context id/readable mapping only
}